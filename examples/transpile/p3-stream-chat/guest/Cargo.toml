[package]
name = "p3-stream-chat"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]
resolver = "2"

[dependencies]
wit-bindgen = { version = "0.58.0", default-features = false, features = ["macros", "async-spawn"] }
