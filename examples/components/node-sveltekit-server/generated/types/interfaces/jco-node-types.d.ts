/** @module Interface jco:node/types@0.1.0 **/
/**
 * Environment variables, as name/value pairs.
 * 
 * TODO(map): change to `map<k, v>` when componentize-js gets support.
 */
export type EnvVars = Array<[string, string]>;
