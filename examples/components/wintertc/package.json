{
    "name": "wintertc",
    "private": true,
    "description": "Exercise portable Web APIs in a TypeScript WebAssembly component",
    "type": "module",
    "scripts": {
        "build": "jco componentize component.ts --wit wit --world-name component --out component.wasm",
        "demo:node": "node demo.ts",
        "demo:deno": "deno run --allow-all demo-deno.ts",
        "demo": "pnpm run demo:node && pnpm run demo:deno",
        "all": "pnpm run build && pnpm run demo"
    },
    "devDependencies": {
        "@bytecodealliance/jco": "workspace:*"
    }
}
