package local:test;

interface foo {
  resource bar {
    constructor(name: string);
  }

  create-bar: func() -> bar;
  consume-bar: func(bar: bar) -> string;
}

world test {
  // The bar resource type is only exported when its defining interface is
  // explicitly exported.
  export foo;

  export hello: func() -> string;
}
