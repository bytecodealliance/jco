{
  "name": "jco",
  "scripts": {
    "build:book": "mdbook build docs",
    "build": "cargo xtask build debug",
    "build:release": "cargo xtask build release",
    "build:types:preview2-shim": "cargo xtask generate wasi-types preview2",
    "build:types:preview3-shim": "cargo xtask generate wasi-types preview3",
    "build:test:components": "pnpm run build:test:components --ws --if-present",
    "fmt": "pnpm run fmt --ws --if-present",
    "fmt:check": "pnpm run fmt:check --ws --if-present",
    "lint": "pnpm run lint --ws --if-present",
    "lint:fix": "pnpm run lint:fix --ws --if-present",
    "test:examples": "pnpm run all --ws --if-present"
  },
  "devDependencies": {
    "@actions/github": "^6.0.1",
    "git-cliff": "^2.10.0",
    "oxfmt": "^0.32.0",
    "oxlint": "^1.47.0"
  }
}
